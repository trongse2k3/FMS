<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F3F4F6"
    tools:context=".MainActivity">

    <!-- Top Bar -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/top_bar_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:layout_marginTop="20dp"
        android:background="@drawable/rounded_blue_top_bar"
        android:padding="16dp"
        android:elevation="4dp">

        <ImageView
            android:id="@+id/lion_king_logo"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/logo_1"
            android:contentDescription="Lion King Logo"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <TextView
            android:id="@+id/text_lion_king"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="LION KING"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_marginStart="8dp"
            app:layout_constraintStart_toEndOf="@id/lion_king_logo"
            app:layout_constraintTop_toTopOf="@id/lion_king_logo"
            app:layout_constraintBottom_toBottomOf="@id/lion_king_logo" />

        <TextView
            android:id="@+id/text_money"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="2000$"
            android:textColor="@android:color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/lion_king_logo"
            app:layout_constraintBottom_toBottomOf="@id/lion_king_logo" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Main Content Area -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        android:paddingTop="120dp"> <!-- Adjust padding top to account for the custom top bar -->

        <ImageView
            android:id="@+id/football_field_image"
            android:layout_width="300dp"
            android:layout_height="200dp"
            android:layout_gravity="top"
            android:layout_marginTop="24dp"
            android:contentDescription="Football Field"
            android:scaleType="centerCrop"
            android:src="@drawable/t_svd_1"
            android:visibility="visible" />

        <ImageView
            android:id="@+id/person_image"
            android:layout_width="200dp"
            android:layout_height="500dp"
            android:layout_gravity="bottom|end"
            android:layout_marginEnd="32dp"
            android:layout_marginBottom="40dp"
            android:clipToOutline="true"
            android:contentDescription="Person"
            android:scaleType="centerCrop"
            android:src="@drawable/t_hlv_1"
            android:visibility="visible" />
    </FrameLayout>

    <!-- FloatingActionButton ở giữa BottomAppBar -->
    <ImageButton
        android:id="@+id/fab_match"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center"
        android:layout_marginBottom="32dp"
        android:background="@drawable/t_ball_1"
        android:elevation="1dp"
        android:contentDescription="Match"
        android:scaleType="centerCrop"/>



    <!-- BottomAppBar + BottomNavigationView -->
    <com.google.android.material.bottomappbar.BottomAppBar
        android:id="@+id/bottom_app_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:backgroundTint="@android:color/white"
        app:fabCradleMargin="10dp"
        app:fabCradleRoundedCornerRadius="30dp"
        app:fabCradleVerticalOffset="10dp"
        app:hideOnScroll="false"
        app:fabAlignmentMode="center">

        <!-- BottomNavigationView nằm trong BottomAppBar -->
        <com.google.android.material.bottomnavigation.BottomNavigationView
            android:id="@+id/bottom_nav_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@android:color/white"
            app:menu="@menu/bottom_nav_menu"
            app:labelVisibilityMode="labeled"
            app:itemIconTint="@drawable/bottom_nav_color_state_list"
            app:itemTextColor="@drawable/bottom_nav_color_state_list" />
    </com.google.android.material.bottomappbar.BottomAppBar>

</androidx.coordinatorlayout.widget.CoordinatorLayout>
