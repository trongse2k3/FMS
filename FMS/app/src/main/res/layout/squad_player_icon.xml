<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@drawable/player_icon_bg"
    android:padding="4dp">

    <ImageView
        android:id="@+id/player_icon_image"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/img_player_1"
        android:scaleType="centerCrop"
        android:contentDescription="Player Icon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/player_icon_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Tên"
        android:textColor="#FFFFFF"
        android:textSize="10sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_marginTop="2dp"
        app:layout_constraintTop_toBottomOf="@id/player_icon_image"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <!-- Nút Xóa Cầu Thủ khỏi đội hình (đã thêm) -->
    <ImageButton
        android:id="@+id/btn_remove_squad_player_icon"
    android:layout_width="24dp"
    android:layout_height="24dp"
    android:background="#FB0000"
    android:src="@drawable/ic_test"
    android:contentDescription="Remove Player"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    android:padding="4dp"
    android:scaleType="fitCenter"
    android:layout_marginEnd="-8dp"
    android:layout_marginTop="-8dp"
    android:visibility="gone" /> <!-- Ẩn ban đầu, chỉ hiện khi có cầu thủ -->

</androidx.constraintlayout.widget.ConstraintLayout>
